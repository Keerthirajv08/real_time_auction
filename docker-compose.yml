

services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - redis
      - db
    env_file:
      - .env

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  db:
    image: postgres:17-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    ports:
      - "5432:5432"

  celery_worker:
    build: .
    command: celery -A base worker -l info -P eventlet
    volumes:
      - .:/app
    depends_on:
      - redis
      - db
    env_file: .env

  celery_beat:
    build: .
    command: celery -A base beat -l info -P eventlet
    volumes:
      - .:/app
    depends_on:
      - redis
      - db
    env_file: .env

volumes:
  postgres_data:

